#!/usr/bin/env bash

set -e

case "$1" in
    configure)
        # Bash completion — always install
        mkdir -p /etc/usr/share/bash-completion/completions
        /opt/bin/cm completion bash > /usr/share/bash-completion/completions/cm

        # Zsh completion — only if zsh is present
        if command -v zsh > /dev/null 2>&1; then
            mkdir -p /usr/share/zsh/site-functions
            /opt/bin/cm completion zsh > /usr/share/zsh/site-functions/_cm
        fi
        ;;
esac

exit 0